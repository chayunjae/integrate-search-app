import styled from "styled-components";

export default styled.section`
  .search-box {
    width: 300px;
    margin: 0 auto;
    text-align: center;
    padding: 3rem 0;
  }

  .search-box .main_input {
    box-sizing: border-box;
    padding: 0.5rem;
    width: 200px;
  }
`;
